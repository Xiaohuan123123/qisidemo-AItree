# 飞书自建应用创建指南

> 用于「栖思 · 笔记整理工具」的「同步到飞书」功能
> 全程约 10 分钟。个人飞书账号即可，不需要企业认证。

---

## 前置准备

- 一个飞书账号（[feishu.cn](https://www.feishu.cn) 注册，个人版免费）
- 建议用**电脑浏览器**操作，后台页面在手机上很难点

---

## 步骤 1：进入开发者后台并创建应用

1. 浏览器打开 **https://open.feishu.cn**
2. 右上角点「**开发者后台**」（若未登录会先要求用飞书扫码登录）
3. 进入后，点「**创建企业自建应用**」
4. 填写应用信息：
   - **应用名称**：栖思笔记（或你喜欢的名字）
   - **应用描述**：把学习笔记同步到飞书云文档
   - **应用图标**：可跳过，会分配默认图标
5. 点「**创建**」

> 创建完成后会直接进入该应用的详情页。

---

## 步骤 2：获取 App ID 和 App Secret ⭐

在应用详情页左侧菜单找到「**凭证与基础信息**」。

页面上会显示两个值：

| 字段 | 说明 | 对应配置项 |
|---|---|---|
| **App ID** | 形如 `cli_a1b2c3d4e5f6g7h8` | `FEISHU_APP_ID` |
| **App Secret** | 一长串随机字符 | `FEISHU_APP_SECRET` |

**先把这两个值复制到记事本备用。**

> ⚠️ App Secret 等同于密码，不要截图发人、不要提交到 Git。
> 本项目 `.gitignore` 已忽略 `.env`，填进去是安全的。

---

## 步骤 3：开通权限 ⭐⭐（最容易出错的一步）

左侧菜单点「**权限管理**」。

### 3.1 先切对页签

页面顶部有两个页签：**「应用身份权限」** 和 **「用户身份权限」**。

👉 **必须点「用户身份权限」**（也叫「以用户身份调用 API」）。

> 这是最大的坑：切错页签开了一堆权限，运行时依然全部 403。

### 3.2 搜索并开通这 4 个权限

在搜索框里逐个搜索添加：

| 权限标识 | 用途 | 是否必须 |
|---|---|---|
| `offline_access` | 获取 refresh_token，让授权长期有效 | ✅ **必须** |
| `docx:document` | 创建、编辑新版文档 | ✅ **必须** |
| `docx:document.block:convert` | Markdown 转文档块 | ✅ **必须** |
| `drive:drive` | 云空间读写（上传/删除兜底） | 建议开通 |

> 少开 `offline_access` 的后果：授权只能用 2 小时，之后每次同步都要重新授权。
> 少开 `docx:document.block:convert` 的后果：同步时转换 markdown 直接报错。

---

## 步骤 4：配置重定向 URL ⭐

左侧菜单找到「**安全设置**」。

在「**重定向 URL**」（部分界面叫「回调地址」）里**添加**：

```
http://localhost:3001/api/feishu-callback
```

如果将来要部署到 Vercel，**再添加一条**（现在可以先不加，用不到）：

```
https://你的域名.vercel.app/api/feishu-callback
```

> 一个应用可以添加多条重定向 URL，不需要建两个应用。
> 地址必须**完全匹配**（含 `http://`、端口号、路径），多一个斜杠都会失败。

> 💡 如果看到另一个叫「**授权回调域**」的字段，**不要和「重定向 URL」搞混**——
> 那个只填域名、不带协议和端口。不确定就跳过，以实际报错为准。

---

## 步骤 5：发布版本 ⭐⭐（不开通权限不生效）

左侧菜单点「**版本管理与发布**」（或「应用发布」）→ 点「**创建版本**」。

1. **版本号**：填 `1.0.0`
2. **可用范围**：选择「**仅自己可用**」或添加你自己为可用成员
   - 必须把你自己加进去，否则你的账号无法使用这个应用
3. **更新说明**：随便填，如「初始版本」
4. 点「**保存**」→ 再点「**申请发布**」

### 关于审核

- **个人版飞书**：你自己就是管理员，通常在后台「**管理后台 → 应用管理**」里能**自助通过**，或者创建版本后自动生效
- **企业飞书**：需要企业管理员审批，可能等几分钟到几小时

> ⚠️ 之后**每次修改权限都要重新创建版本并发布**，否则新权限不生效。
> 这是排查「明明配好了却说没权限」时的第一检查项。

---

## 步骤 6：填入配置并验证

1. 打开项目里的 `.env` 文件（没有就复制 `.env.example` 改名）
2. 填入步骤 2 拿到的值：

```bash
FEISHU_APP_ID=cli_你复制的AppID
FEISHU_APP_SECRET=你复制的AppSecret
FEISHU_REDIRECT_URI=http://localhost:3001/api/feishu-callback
```

3. 重启服务器：

```bash
node server.js
```

4. 启动日志里应该看到：

```
📄 飞书同步:         ✅ 已配置
```

5. 浏览器打开 http://localhost:3001 → 「我的」→「设置」
6. 「连接飞书账号」应显示为**未连接**，点「连接」按钮
7. 弹出飞书授权页 → 点「授权」→ 窗口自动关闭 → 提示「已连接飞书账号 ✅」

**到这里就成功了。** 之后在笔记详情页点「📄 同步到飞书」，笔记就会出现在你的飞书云空间。

---

## 常见问题排查

| 现象 | 原因 | 解决 |
|---|---|---|
| 启动日志显示「未配置」 | `.env` 没填或没重启 | 填好后重启 `node server.js` |
| 点连接没反应/窗口被拦 | 浏览器拦截弹窗 | 允许本站弹窗后重试 |
| 授权页报「重定向 URL 不匹配」 | 地址没配或没完全匹配 | 检查步骤 4，注意 `http://` 和端口 |
| 授权后提示「授权校验未通过」 | state 超时（超过10分钟） | 重新点一次连接 |
| 同步报「飞书拒绝了操作」 | 权限没开全 / 没发版本 / 切错页签 | 回步骤 3 检查，**并重新发布版本** |
| 同步报「未授权或授权已过期」 | refresh_token 失效 | 设置页点「断开」再重新「连接」 |
| 文档建出来了但内容为空 | markdown 转换失败 | 看服务器日志的 `[Feishu]` 报错行 |

---

## 安全检查清单

创建完成后确认：

- [ ] `.env` 文件**没有**被提交到 Git（本项目已通过 `.gitignore` 忽略）
- [ ] `.feishu-token.json`（授权后自动生成）**没有**被提交
- [ ] App Secret 没有出现在任何截图或聊天记录里
- [ ] 访问 `http://localhost:3001/.env` 返回 **404**（本项目已修复此漏洞）

---

## 附：为什么要这么多步骤

飞书把「应用身份」和「用户身份」严格分开：

- **应用身份**：文档建在应用自己的空间里，用户找不到
- **用户身份**：文档建在**你自己的**云空间里，你能正常查看、编辑、分享

本项目用的是**用户身份**，所以必须：开「用户身份权限」页签的权限 + 配置重定向 URL（走 OAuth 授权流程拿到你的授权）+ 发布版本（权限变更才会生效）。

三步缺一不可，这也是为什么步骤 3、4、5 都标了星号。
